// Ez itt a move() - ott csúszkálunk, mint az őrültek!
public void move() {
    // Húzzunk egy kis máját a pályán
}

// Ez a fight() - vetkőzés hősök között!
public void fight() {
    // Pindur-botmérkőzéseket szabadítsunk el
}

// Íme a levelUp() - szintugrások, mint a kiscicák!
public void levelUp() {
    // Húzzuk fel a szintet egy kicsit
}

// A loopban addig körözünk, míg le nem pörgeted az utolsó életed!
for (int i = 0; i < lives; i++) {
    // Zúzzuk agyba-főbe ezeket az ellenségeket!
}